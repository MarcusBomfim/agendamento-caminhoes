# Porto Agenda Web

Interface em React e TypeScript do sistema Porto Agenda.

## Comandos

```powershell
npm.cmd install
npm.cmd run dev
npm.cmd run lint
npm.cmd test
npm.cmd run build
```

Configure o endereço da API em `.env` usando `VITE_API_URL`. Consulte o README da raiz para executar o sistema completo.
